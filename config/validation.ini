[error.usermodel]

role.required = Role is required.
password.required = Password is required.
password.min_len = Minimum Password Length is 5.
password.max_len = Maximum Password Length is 20.
phone_username.required = Phone no is required.
phone_username.unique = This phone number already exists.
phone_username.regex = Given phone number is not valid.
account_status.required = Account status is required
account_status.exact_len = Account status can be 1 or 0.

[error.userprofilemodel]

name.required = Name is required.
name.min_len = Name must have a minimum length of 5.
name.max_len = Name exceeds maximum length 128.
name.alpha_space = Name must not contain any numerical value.
nid_no.required = NID is required.
nid_no.unique = This NID already exists.
nid_no.max_len = Length of NID must not be more than 20.
designation_id.required = Designation ID is required.
salary.required = Salary is required.
salary.numeric = Salary should only contain numeric values.
salary.max_len = Salary exceeds maximum length 7.
shop_id.required = Shop ID is required.

[error.usertypemodel]

name.required = User type is required.
name.unique = User type should be unique.
name.alpha = User type should only contain alphabets.
name.max_len = User type exceeds maximum length 50.

[error.categorymodel]

name.required = Category name is required.
name.unique = Category should be unique.
name.alpha = Category name should only contain alphabets.
name.max_len = Category name exceeds maximum length 50.
description.required = Category description is required.
description.max_len = Category description exceeds maximum length 500.
featured.required = Featured is required.
featured.exact_len = Featured can be 1 or 0.
parent_id.required = Parent ID is required.

[error.suppliermodel]

name.required = Supplier name is required.
name.unique = this supplier already exists.
name.max_len = Supplier name exceeds maximum length of 100.
email.unique = This email already exists.
email.valid_email = Email is not valid.
phone_no.required = Phone no is required.
phone_no.unique = This phone no already exists.
phone_no.regex = The phone no. is not valid.
address.required = Address is required.
address.max_len = Address exceeds maximum length of 100.
company_name.max_len = Company name exceeds maximum length of 50.
company_phone_no.regex = The phone no. is not valid.
company_address.max_len = Company address exceeds maximum length of 100.
bank_account_info.max_len = Company address exceeds maximum length of 200.
ref_comment.max_len = Reference comment exceeds maximum length of 500.

[error.departmentmodel]

name.required = Department name is required.
name.unique= This department name already exists.
name.alpha_space = Department name can not contain numerical value.
name.max_len = Department name exceeds maximum length 100.

[error.shopmodel]

name.required = Shop name is required.
name.unique= This shop name already exists.
name.alpha = Shop name can not contain space & numerical values.
name.max_len = Shop name exceeds maximum length 50.
name.min_len = Length of shop name must be at least 5.

[error.branchmodel]

shop_id.required = Shop ID is required.
name.required = Branch name is required.
name.unique = This branch name already exists.
name.alpha = Branch name contain space & numerical values.
location.required = Branch location is required.
location.unique = Branch in this location already exists.

[error.brandmodel]

name.required = Brand name is required.
name.unique= This brand name already exists.
name.alpha = Brand name can not contain space & numerical values.
name.max_len = Brand name exceeds maximum length 50.
description.required = Brand description is required.
description.max_len = Brand description exceeds maximum length 500.

[error.saleskpimodel]

user_id.required = User ID is required.
sales_volume.required = Sales Volume is required.
sales_volume.numeric = Sales Volume can only contain numeric values.

[error.salestypemodel]

type.required = Sales type is required.
type.unique = This sales type already exists.
type.alpha_space = Sales type must not contain numeric values.
type.max_len = Sales type exceeds maximum length 50.

[error.customermodel]

name.required = Brand name is required.
name.alpha_space = Customer name can not contain numerical values.
name.max_len = Brand name exceeds maximum length 100.
email.valid_email = Email is not valid.
phone_no.required = Phone no is required.
phone_no.unique = This phone no already exists.
phone_no.regex = The phone no is not valid.
address.max_len = Address exceeds maximum length of 100.
company_name.max_len = Company name exceeds maximum length of 100.

[error.logtypemodel]

name.required = Log type is required.
name.unique= This log type already exists.
name.max_len = Log type exceeds maximum length 50.

[error.transactiontypemodel]

name.required = Transaction type is required.
name.unique= This transaction type already exists.
name.max_len = Transaction type exceeds maximum length 50.

[error.transfertypemodel]

name.required = Transfer type is required.
name.unique= This transfer type already exists.
name.max_len = Transfer type exceeds maximum length 50.